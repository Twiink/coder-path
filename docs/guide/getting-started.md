# 快速开始

## 安装

```bash
# 克隆仓库
git clone https://github.com/Twiink/coder-path.git

# 安装依赖
npm install

# 启动开发服务器
npm run docs:dev
```

## 本地开发

```bash
npm run docs:dev
```

开发服务器将在本地启动，可以实时预览修改。

## 构建

```bash
npm run docs:build
```

构建产物将生成在 `docs/.vitepress/dist` 目录。

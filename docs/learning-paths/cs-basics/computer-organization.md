# 计算机组成原理学习路线

你写的每一行代码,最终都会变成 0 和 1 的电信号,在一块硅片上跑完它的一生。学计算机组成原理,不是为了让你去造 CPU,而是为了让你写代码时心里有一张"硬件地图":知道 `0.1 + 0.2` 为什么不等于 `0.3`,知道为什么遍历数组比遍历链表快,知道为什么 32 位系统内存上限是 4G。

这条线从"数据怎么表示"讲到"一条指令怎么变成电信号",建议按顺序走,前面是后面的地基。

## 第一站:数据是怎么表示的

计算机里只有 0 和 1,所以一切从**进制**开始:二进制、八进制、十六进制(后两者是二进制的"缩写",十六进制一位正好对应四位二进制,所以看内存地址和颜色值都用它)。

整数用**补码**存储,这是计算机最优雅的设计之一:有了补码,加法和减法可以共用一套电路,`1 - 1` 就是 `1 + (-1)`,溢出进位自然丢弃。代价是新手常见的**整型溢出**:有符号 32 位整数最大是 `2^31 - 1`,再加 1 会翻成负数——还记得 JavaScript 里 `Number.MAX_SAFE_INTEGER` 和 `BigInt` 为什么存在吗?就是补码和浮点表示的锅。

小数更精彩。**IEEE 754 浮点数**用"符号位 + 指数 + 尾数"三段表示,精度有限,于是有了那句名言:`0.1 + 0.2 !== 0.3`(在 JS 里是真的)。这不是 bug,是任何二进制浮点都绕不开的宿命——金融计算要用十进制定点数或专门的 decimal 库,就是这个原因。

最后是**字符编码**:ASCII 只有 128 个字符,装不下中文,于是有了 Unicode(给每个字符一个编号)和 UTF-8(变长存储,兼容 ASCII,网络传输的主流)。乱码的本质,就是"用 A 编码写、用 B 编码读"。

## 第二站:从逻辑门到加法器

再往下一层,是**逻辑门**:与、或、非——初中物理的开关电路就能搭出来。把门电路组合起来,能做出**半加器、全加器**,再做出一整块能做算术和逻辑运算的电路。这一站不需要你会设计电路,只需要建立一个信念:计算机里一切"智能",都是亿万次"开关"的组合。CPU 就是一块超级复杂的门电路集合。

## 第三站:CPU 是怎么干活的

CPU 执行的是**指令**(机器码),每条指令由**指令集架构(ISA)** 定义——x86(PC/服务器主流)、ARM(手机/笔记本)、RISC-V(开源新贵)是三大门派,指令集不同,软件不能直接通用,这就是为什么 macOS 换芯片后老软件要重编译或转译。

一条指令的旅程是**指令周期**:取指(从内存把指令取到寄存器)→ 译码(弄明白这条指令要干嘛)→ 执行(算!)→ 回写(存结果)。现代 CPU 用**流水线**让多条指令重叠执行(像工厂流水线),但遇到分支跳转会产生"冒险",于是有**分支预测**——猜错了就推倒重来,这就是为什么"代码里少写难以预测的分支"能变快。

两个常见误区提前排雷:主频(3GHz)不等于性能,还要看每个时钟周期能干多少活(IPC);核心数也不是越多越好,程序得能并行才有用——还记得并发和并行的区别吗,那是操作系统的主场。

## 第四站:存储层次——快与便宜的博弈

没有一种存储既快又便宜又大,所以计算机搞了**存储层次**:寄存器(纳秒级,容量最小)→ L1/L2/L3 缓存 → 内存 → SSD/磁盘(毫秒级,容量最大)。每往下一层,容量大一个数量级,速度慢一个数量级。

缓存能生效靠的是**局部性原理**:时间局部性(刚用过的数据很快再用)和空间局部性(用了一个字节,附近字节很快也会用)。CPU 缓存按**缓存行**(通常 64 字节)加载,所以**顺序遍历数组极快,跳着访问链表节点极慢**——这就是数据结构那课"数组 vs 链表"在硬件层面的答案,两条路线在这里第一次握手。

多线程还有个隐藏杀手叫**伪共享**:两个线程改不同的变量,但它们恰好挤在同一个缓存行里,互相拖慢。高端优化会故意做"缓存行填充"来隔离,知道这个概念,你以后排查多线程性能问题时就不会一脸懵。

## 第五站:内存与地址

内存是字节编址的一长条,CPU 按**地址**访问。这里有个细节叫**字节序**:一个多字节整数(比如 `0x12345678`),内存里是"高位在前"还是"低位在前"?x86 是小端,网络协议规定大端。所以二进制协议都要显式规定字节序,而 JSON 这种文本格式压根没这个问题——这也是为什么跨语言通信用文本格式更省心。

另一个概念是**内存对齐**:CPU 按字(4/8 字节)读内存,数据没对齐可能要读两次,所以编译器会在结构体里"偷偷塞填充字节"。结构体字段顺序影响内存占用,这是 C/C++ 工程师的日常,其他语言大多帮你处理好了。

## 第六站:输入输出——CPU 怎么和外界打交道

键盘、硬盘、网卡都是外设,CPU 跟它们通信靠**总线**。三种方式层层进化:**轮询**(CPU 不停问"好了没",浪费)、**中断**(设备好了主动喊 CPU,像有人按门铃)、**DMA**(数据搬运直接由 DMA 控制器完成,不占用 CPU,大文件传输全靠它)。你每次读写文件、收发网络包,背后都是这套机制在跑——操作系统把这一切封装成了"文件"和"套接字",这就是下一站衔接操作系统的地方。

## 第七站:汇编语言初体验

别被吓到,这一站不求会写,只求"不怕看"。汇编是机器码的助记符版本:`mov`(搬数据)、`add`/`sub`(加减)、`cmp`(比较)、`jmp`(跳转)、`call`/`ret`(调用函数和返回)。函数调用的硬件真相在这里揭开:**调用栈**——`call` 把返回地址压栈,函数局部变量在栈上分配,返回时弹栈,所以递归太深会**栈溢出**(栈空间有限),所以栈上变量函数结束就自动"释放"。

进阶玩家可以用 `objdump` 反汇编看看自己写的代码长什么样,或者用调试器单步看寄存器变化——你会第一次直观感受到"高级语言的一行,是底下的几十条指令"。

## 第八站:从源代码到电信号

把整条线串起来看一次:**高级语言**(if、for、函数)→ 编译器翻译成**汇编** → 汇编器变成**机器码** → CPU 取指执行 → 电信号在门电路里流转。解释型语言(JavaScript、Python)不提前编译,而是运行时由**解释器/JIT** 边翻译边执行——JIT 会把热点代码编译成机器码,所以"解释型语言慢"早已不是铁律。

再往上,操作系统接管了硬件:进程调度、虚拟内存、文件系统——CPU 和内存怎么被"分时复用",请看下一站 [操作系统学习路线](/learning-paths/cs-basics/operating-systems)。

## 通关标准

能向别人解释清楚这几件事,计算机组成原理就算入门了:`0.1 + 0.2` 为什么不等于 `0.3`、负数为什么要用补码、为什么遍历数组比链表快(结合缓存行)、32 位系统为什么最多 4G 内存、一条 `if` 语句在硬件层面大致发生了什么、函数调用为什么用栈。

## 学完去哪

组成原理是操作系统的前置课,紧接着学 [操作系统学习路线](/learning-paths/cs-basics/operating-systems);想看看"数组为什么快"在代码层面的印证,回 [数据结构与算法学习路线](/learning-paths/cs-basics/data-structures-algorithms);语言的数字精度问题可以在 [JavaScript 学习路线](/learning-paths/frontend/javascript) 和 [Java 学习路线](/learning-paths/languages/java) 里看到实际案例。硬件知识平时用不上,但它决定了你能走多深——尤其是性能优化和底层排错的时候。

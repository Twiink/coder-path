[permissions]
allow = ["Bash=cd /Users/mac/ProjectStation/coder-path && python3 - <<'EOF'\np = 'docs/learning-paths/ai/langchain.md'\nlines = open(p, encoding='utf-8').read().splitlines()\nfences = [i for i, ln in enumerate(lines) if ln.startswith('```')]\nprint('总行数', len(lines), '代码块', len(fences)//2)\ncur = ''\nfor k in range(0, len(fences), 2):\n    a, b = fences[k], fences[k+1]\n    for j in range(a, -1, -1):\n        if lines[j].startswith('#'):\n            cur = lines[j].strip(); break\n    head = next((l.strip()[:55] for l in lines[a+1:b] if l.strip()), '')\n    print(f'{k//2+1:2d} | {cur[:34]:36s} | {head}')\nprint('----- 非代码骨架 -----')\nins = False\nfor i, ln in enumerate(lines, 1):\n    if ln.startswith('```'):\n        ins = not ins; continue\n    if not ins and ln.strip():\n        print(f'{i}: {ln}')\nEOF", "Bash=cd /Users/mac/ProjectStation/coder-path && grep -rln '^\\`\\`\\`' docs/learning-paths/frontend/ && echo '有代码块' || echo 'frontend 无代码块'; python3 -c \"\nimport glob, os, re\nbroken = total = 0\nfor f in glob.glob('docs/learning-paths/**/*.md', recursive=True):\n    for m in re.finditer(r'\\[([^\\]]*)\\]\\((/[^)]+)\\)', open(f, encoding='utf-8').read()):\n        if not m.group(2).startswith('/learning-paths/'): continue\n        total += 1\n        rel = 'docs' + m.group(2)\n        if not any(os.path.exists(c) for c in (rel + '.md', rel + '/index.md')): broken += 1\nprint('links:', total, 'broken:', broken)\n\""]
